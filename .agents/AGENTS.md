# 프로젝트 규칙

- **배경 인물**: 배경에 들어가는 이미지나 영상 등에 인물이 등장할 경우, 항상 **한국인** 캐릭터/인물로 묘사되도록 설정할 것.

- **자동 커밋 및 푸시**: 코드를 수정하고 턴을 종료(stop)하기 전에, 항상 `git add`, `git commit`, `git push`를 실행하여 원격 저장소를 최신 상태로 유지할 것 (사용자가 명시적으로 금지하지 않는 한).
